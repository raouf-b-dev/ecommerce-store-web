import { PageHeader } from '@/components/layout/page-header';

export default function HomePage() {
  return (
    <PageHeader
      title="Home"
      description="Customer storefront for the ecommerce API. Catalog, cart, and checkout are not wired yet."
    />
  );
}
