# Storefront visual assets (Phase 13)

Capture from `npm run dev:mock` first, then re-verify against a live API after [`RELEASE-GATE.md`](../RELEASE-GATE.md) checks pass.

## Planned assets

- Walkthrough WebP/GIF: browse, add to cart, checkout confirmation
- Retina stills: home, product detail, cart/checkout
- README hero embedding the walkthrough

## Optional suite clip

For a multi-app demo, run the API plus companion admin and storefront repos together. A 60-90 second silent clip can end on the admin WebSocket toast when checkout completes; verify that moment from the admin repository docs, not from this repo.
